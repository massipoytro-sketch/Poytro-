import React from 'react';
import { navGroups } from '../data/mock';

export function Logo(){return <div className="brand"><span className="brand-mark">G</span><span>GainiRen</span></div>}
export function Icon({children}){return <span className="icon" aria-hidden="true">{children}</span>}
export function Sidebar({page,setPage}){return <aside className="sidebar"><Logo/><div className="nav-groups">{navGroups.map(g=><div className="nav-group" key={g.label}><div className="nav-label">{g.label}</div>{g.items.map(([id,label,icon])=><button key={id} className={'nav-item '+(page===id?'active':'')} onClick={()=>setPage(id)}><Icon>{icon}</Icon><span>{label}</span></button>)}</div>)}</div><div className="sidebar-user"><div className="avatar">A</div><div><b>Ahmed</b><small>Member</small></div><span className="online"/></div></aside>}
export function Topbar({page}){return <header className="topbar"><div className="mobile-logo"><Logo/></div><div className="crumb"><span>GainiRen</span><i>/</i><b>{page.replace(/(^|_)(\w)/g,(_,a,b)=>b.toUpperCase())}</b></div><div className="top-actions"><button className="search">⌕ <span>Search anything</span><kbd>⌘ K</kbd></button><button className="round">♢</button><button className="top-avatar">A</button></div></header>}
export function MobileNav({page,setPage}){const items=[['dashboard','⌂','Home'],['offers','✦','Earn'],['withdraw','↗','Cash'],['store','◆','Store'],['profile','●','Me']];return <nav className="mobile-nav">{items.map(([id,icon,label])=><button className={page===id?'active':''} key={id} onClick={()=>setPage(id)}><Icon>{icon}</Icon><span>{label}</span></button>)}</nav>}
