{
  "name": "poytro-rewards",
  "version": "1.0.0",
  "private": true,
  "description": "Poytro premium rewards web app",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html styles.css premium.css design-final.css reference-final.css app.js dist/ && if [ -d assets ]; then cp -r assets dist/assets; fi",
    "dev": "python3 -m http.server 5173",
    "preview": "python3 -m http.server 4173 --directory dist"
  },
  "dependencies": {},
  "devDependencies": {}
}
